@using GymManagement.Presentation.WebApp.ApiClients
@rendermode InteractiveServer

@if (_authenticatedUser.IsAuthenticated)
{
    <div class="sidebar">
        <NavMenu />
    </div>
}
else if (!_navigationManager.Uri.Contains("/login"))
{
    _authManager.RedirectToLogin();
}